import { FirstCapsDirective } from './first-caps.directive';

describe('FirstCapsDirective', () => {
  it('should create an instance', () => {
    const directive = new FirstCapsDirective();
    expect(directive).toBeTruthy();
  });
});
